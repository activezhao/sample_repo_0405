syntax = "proto3";
//package code generated by schemakit  DO NOT EDIT.
package sample_module_0405.sample_package_0405;


import "google/api/annotations.proto";
import "validate/validate.proto";

// 设置签名请求参数
message SetSignRequest {
    // 用户名
    string user = 1;
    // 签名内容
    string sign = 2;
}

// 获取签名请求参数
message GetSignRequest {
    // 用户名
    string user = 1;
}

// 返回结果
message SignReply {
    // 返回消息
    string msg = 1;
    // 错误码
    string code = 2;
    // 返回的数据
    SignInfo data = 3;
}

// 签名信息
message SignInfo {
    // 签名内容
    string sign = 1;
    // 签名更新时间
    string update_time = 2;
}

// 签名服务
service Zhaoh0405 {
    // 设置签名
    rpc SetSign (SetSignRequest) returns (SignReply){
        option (google.api.http) = {
            post: "/api/v1/sign"
            body: "*"
        };
    };

    // 获取签名
    rpc GetSign (GetSignRequest) returns (SignReply){
        option (google.api.http) = {
            get: "/api/v1/sign"
        };
    };
}